"""
Project scripts and CLI entrypoints.

This file exists so `scripts.*` imports resolve consistently across environments
(i.e., `scripts` is treated as a regular package instead of an implicit namespace).
"""


